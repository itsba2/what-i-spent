const Main = ({ children }) => {
    return (
        <main className="bg-background dark:bg-backgroundDark min-h-screen flex flex-col items-center justify-between w-full">{children}</main>
    )
}

export default Main

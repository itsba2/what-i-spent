rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /user {
      allow read, write;
    }
    match /expense {
      allow read, write: if request.auth != null;
    }
    match /earning {
      allow read, write: if request.auth != null;
    }
  }
}